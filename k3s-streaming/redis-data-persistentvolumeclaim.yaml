apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: redis-streaming-data
  name: redis-streaming-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
