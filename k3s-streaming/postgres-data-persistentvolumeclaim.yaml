apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: postgres-streaming-data
  name: postgres-streaming-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
